@if (HasModel)
{
    <EditForm EditContext="@_editContext" class="">
        <DataAnnotationsValidator />

        @ChildContent

        <NavigationButtons ShowPrevious="@HasPrevious" PreviousHandler="@HandlePrevious" ShowNext="@HasNext" NextHandler="@HandleNext"></NavigationButtons>
    </EditForm>
}
else
{
    @ChildContent

    <NavigationButtons ShowPrevious="@HasPrevious" PreviousHandler="@HandlePrevious" ShowNext="@HasNext" NextHandler="@HandleNext"></NavigationButtons>
}
